"""vm — Execution-VM failure analysis & repair infrastructure.

Package root for the live repair-pipeline modules
(``failure_classifier``, ``repair_registry``, ``repair_strategies``,
``models``, ``classification``) consumed by ``agent/tool_registry``.
"""
